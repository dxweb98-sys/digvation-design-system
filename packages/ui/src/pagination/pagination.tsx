import { cn } from '../cn';
export interface PaginationProps { page: number; totalPages: number; onChange: (page:number)=>void; siblingCount?: number; disabled?: boolean; className?: string; }
export function getPaginationItems(page:number,total:number,sibling=1):(number|'ellipsis')[]{ if(total<=7) return Array.from({length:Math.max(total,0)},(_,i)=>i+1); const start=Math.max(2,page-sibling), end=Math.min(total-1,page+sibling); const items:(number|'ellipsis')[]=[1]; if(start>2) items.push('ellipsis'); for(let i=start;i<=end;i++) items.push(i); if(end<total-1) items.push('ellipsis'); items.push(total); return items; }
export function Pagination({ page,totalPages,onChange,siblingCount=1,disabled=false,className }:PaginationProps){ const go=(next:number)=>{ if(!disabled && next>=1 && next<=totalPages && next!==page) onChange(next); }; return <nav aria-label="Pagination" className={cn('flex items-center gap-1',className)}><button type="button" aria-label="Previous page" disabled={disabled||page<=1} onClick={()=>go(page-1)} className="size-9 rounded-lg border border-[var(--color-border)] text-sm text-[var(--color-text-muted)] hover:bg-[var(--color-surface-muted)] disabled:opacity-40">‹</button>{getPaginationItems(page,totalPages,siblingCount).map((item,index)=>item==='ellipsis'?<span key={`e-${index}`} className="grid size-9 place-items-center text-[var(--color-text-muted)]">…</span>:<button type="button" key={item} aria-current={item===page?'page':undefined} onClick={()=>go(item)} className={cn('size-9 rounded-lg text-sm font-medium transition-colors',item===page?'bg-[var(--color-brand)] text-white':'border border-[var(--color-border)] text-[var(--color-text)] hover:bg-[var(--color-surface-muted)]')}>{item}</button>)}<button type="button" aria-label="Next page" disabled={disabled||page>=totalPages} onClick={()=>go(page+1)} className="size-9 rounded-lg border border-[var(--color-border)] text-sm text-[var(--color-text-muted)] hover:bg-[var(--color-surface-muted)] disabled:opacity-40">›</button></nav>; }
